function same(firstArr, secondArr) {
  if (firstArr.length !== secondArr.length) {
    return false;
  }

  // square the first Arr and compare values along with a used param
  // transform secondArr to an object { value: val, squared: sq, used: false}
  // iterate the firstArr and if value found set used to true.
}
